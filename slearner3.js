// Write a function that takes the base and height of a triangle and return its area.


const triangleCalculator = (base, height) => {

    let trisngleArea = (base * height) / 2;
    console.log(trisngleArea);
}

triangleCalculator(10, 10)